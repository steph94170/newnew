{% extends 'base.html.twig' %}

{% block body %}
    <div class='container my-5'>
        {# <h1>{{category.name}}</h1> #}
       
        <div class="row">
            <div class="col-md-6 ">
                   <img src="/uploads/{{product.illustration}}" class="img-fluid" alt="{{product.name}}" >
            </div>
             <div class="col-md-6 ">
                    <small><a class=" fw-bold text-dark " href="{{path('app_category', {slug : product.category.slug })}}">{{product.category.name}}:</a> {{product.name}}</small>
                   <h1>{{product.name}}</h1>
                   <span class='d-block mb-3'>{{product.priceWt|price}}</span>
                   {{product.description|raw}} 
                   <a href="{{path('app_cart_add', {id : product.id})}}"class="btn btn-success w-100 mt-3">Ajouter au panier</a>
            </div>
        </div>
    </div>
{% endblock %}
 {#  {{product.description|raw}}  pour faire disparaitre les balises html dans la description du produit#}

